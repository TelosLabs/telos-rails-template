name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - "**"

env:
  RAILS_ENV: test
  CI: true

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    env:
      RUBOCOP_CACHE_ROOT: tmp/rubocop
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true
      - name: Prepare RuboCop cache
        uses: actions/cache@v5
        env:
          DEPENDENCIES_HASH: ${{ hashFiles('.ruby-version', '**/.rubocop.yml', '**/.rubocop_todo.yml', 'Gemfile.lock') }}
        with:
          path: ${{ env.RUBOCOP_CACHE_ROOT }}
          key: rubocop-${{ runner.os }}-${{ env.DEPENDENCIES_HASH }}-${{ github.ref_name == github.event.repository.default_branch && github.run_id || 'default' }}
          restore-keys: |
            rubocop-${{ runner.os }}-${{ env.DEPENDENCIES_HASH }}-
      - name: Run RuboCop
        run: bundle exec rubocop

  security:
    name: Security
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true
      - name: Run Brakeman
        run: bundle exec brakeman -w3 --no-pager
      - name: Run bundle-audit
        run: bundle exec bundle-audit check --update
      - name: Audit importmap packages
        run: bin/importmap audit

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true
      - name: Prepare database
        run: bundle exec rails db:migrate
      - name: Run RSpec
        run: bundle exec rspec

  e2e:
    name: E2E
    runs-on: ubuntu-latest
    env:
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true
      - name: Check OpenAI key
        id: openai_key
        run: |
          if [ -n "$OPENAI_API_KEY" ]; then
            echo "has_key=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_key=false" >> "$GITHUB_OUTPUT"
            echo "OPENAI_API_KEY is not configured; skipping E2E run."
          fi
      - name: Setup Node.js
        if: steps.openai_key.outputs.has_key == 'true'
        uses: actions/setup-node@v6
        with:
          node-version: "22"
      - name: Install agent-tests dependencies
        if: steps.openai_key.outputs.has_key == 'true'
        run: npm install
        working-directory: agent-tests
      - name: Install Playwright Chromium
        if: steps.openai_key.outputs.has_key == 'true'
        run: npx playwright install chromium --with-deps
        working-directory: agent-tests
      - name: Run E2E tests
        if: steps.openai_key.outputs.has_key == 'true'
        run: bin/e2e
      - name: Upload failure screenshots
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: e2e-failure-screenshots
          path: agent-tests/screenshots/
      - name: Upload failure report
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: e2e-failure-report
          path: agent-tests/failures.md

  summary:
    name: Summary
    runs-on: ubuntu-latest
    if: always()
    needs: [lint, security, test, e2e]
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      - name: Generate CI summary
        run: |
          BRANCH="${GITHUB_HEAD_REF:-$GITHUB_REF_NAME}"
          COMMIT="${GITHUB_SHA::7}"
          TIMESTAMP="$(date -u +"%Y-%m-%d %H:%M:%S UTC")"

          lint_status="${{ needs.lint.result }}"
          security_status="${{ needs.security.result }}"
          test_status="${{ needs.test.result }}"
          e2e_status="${{ needs.e2e.result }}"

          check() {
            if [ "$1" = "success" ] || [ "$1" = "skipped" ]; then
              echo "- [x] $2"
            else
              echo "- [ ] $2"
            fi
          }

          {
            echo "# CI Summary"
            echo ""
            echo "| Field | Value |"
            echo "|---|---|"
            echo "| Branch | \`${BRANCH}\` |"
            echo "| Commit | \`${COMMIT}\` |"
            echo "| Timestamp | ${TIMESTAMP} |"
            echo ""
            echo "## Results"
            echo ""
            check "$lint_status" "lint"
            check "$security_status" "security"
            check "$test_status" "test"
            check "$e2e_status" "e2e"
            echo ""

            if [ "$lint_status" = "success" ] && [ "$security_status" = "success" ] && \
               [ "$test_status" = "success" ] && [ "$e2e_status" = "success" -o "$e2e_status" = "skipped" ]; then
              echo "**Status: ALL PASSED**"
            else
              echo "**Status: FAILED**"
            fi
          } > ci_summary.md

          cat ci_summary.md
      - name: Upload CI summary
        uses: actions/upload-artifact@v7
        with:
          name: ci-summary
          path: ci_summary.md
