name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - "**"

env:
  RAILS_ENV: test
  CI: true

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    env:
      RUBOCOP_CACHE_ROOT: tmp/rubocop
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true
      - name: Prepare RuboCop cache
        uses: actions/cache@v5
        env:
          DEPENDENCIES_HASH: ${{ hashFiles('.ruby-version', '**/.rubocop.yml', '**/.rubocop_todo.yml', 'Gemfile.lock') }}
        with:
          path: ${{ env.RUBOCOP_CACHE_ROOT }}
          key: rubocop-${{ runner.os }}-${{ env.DEPENDENCIES_HASH }}-${{ github.ref_name == github.event.repository.default_branch && github.run_id || 'default' }}
          restore-keys: |
            rubocop-${{ runner.os }}-${{ env.DEPENDENCIES_HASH }}-
      - name: Run RuboCop
        run: bundle exec rubocop

  security:
    name: Security
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true
      - name: Run Brakeman
        run: bundle exec brakeman -w3 --no-pager
      - name: Run bundle-audit
        run: bundle exec bundle-audit check --update
      - name: Audit importmap packages
        run: bin/importmap audit

  code_quality:
    name: Code Quality
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true
      - name: Run RubyCritic
        run: bundle exec rubycritic --no-browser --minimum-score 85 app/controllers app/helpers app/jobs app/mailers app/models app/services app/policies app/builders app/decorators

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true
      - name: Prepare database
        run: bundle exec rails db:migrate
      - name: Run RSpec
        run: bundle exec rspec

  summary:
    name: Summary
    runs-on: ubuntu-latest
    if: always()
    needs: [lint, security, code_quality, test]
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      - name: Generate CI summary
        run: |
          BRANCH="${GITHUB_HEAD_REF:-$GITHUB_REF_NAME}"
          COMMIT="${GITHUB_SHA::7}"
          TIMESTAMP="$(date -u +"%Y-%m-%d %H:%M:%S UTC")"

          lint_status="${{ needs.lint.result }}"
          security_status="${{ needs.security.result }}"
          test_status="${{ needs.test.result }}"
          code_quality_status="${{ needs.code_quality.result }}"

          check() {
            if [ "$1" = "success" ] || [ "$1" = "skipped" ]; then
              echo "- [x] $2"
            else
              echo "- [ ] $2"
            fi
          }

          {
            echo "# CI Summary"
            echo ""
            echo "| Field | Value |"
            echo "|---|---|"
            echo "| Branch | \`${BRANCH}\` |"
            echo "| Commit | \`${COMMIT}\` |"
            echo "| Timestamp | ${TIMESTAMP} |"
            echo ""
            echo "## Results"
            echo ""
            check "$lint_status" "lint"
            check "$security_status" "security"
            check "$code_quality_status" "code quality"
            check "$test_status" "test"
            echo ""

            if [ "$lint_status" = "success" ] && [ "$security_status" = "success" ] && \
               [ "$code_quality_status" = "success" ] && \
               [ "$test_status" = "success" ]; then
              echo "**Status: ALL PASSED**"
            else
              echo "**Status: FAILED**"
            fi
          } > ci_summary.md

          cat ci_summary.md
      - name: Upload CI summary
        uses: actions/upload-artifact@v7
        with:
          name: ci-summary
          path: ci_summary.md
